// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
        domains: ['i.ibb.co', 'aceternity.com', 'images.unsplash.com'],
    },
}

export default nextConfig